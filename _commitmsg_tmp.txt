精简：删死代码、消除与 calculator 重复的算法

- 设置窗「自动计算」改调 calculator 的 calc_auto_today_earned /
  calc_auto_month_earned，删掉内联重复的 60 行；1344 组输入验证两式
  完全一致，改动前后探针输出逐字节相同
- 删 _base_monthly、hotkey_to_string、unregister_all、HotkeyFilter._ids
  及 9 个未使用导入；main 退出不再手工注销热键（进程退出系统自动回收）
- _load_monthly 改用 load_json；GUID 改用 uuid.UUID(...).bytes_le
- styles 删掉算而不用、从不插值的 pad_h/pad_v
- 星期偏移归一化一行；本月进度复用 count_workdays_before
- 4 个自检脚本全部 exit=0
